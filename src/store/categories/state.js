/* eslint-disable no-console */
// import { resolve } from 'path'
// // const inject = require('require-all')
// import * as inject from 'require-all'
// const categories = inject({
//   dirname: resolve('./src/utils/data/categories'),
//   recursive: true,
// })

// const cate = Object.values(categories)

export default function (state) {
  return {
    categories: 'cate',
  }
}
